<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.practica1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Practica1">
        <activity android:name=".VistaColores"
            android:label="@string/colores_usados"></activity>
        <activity android:name=".VistaFiguras"
        android:label="@string/figuras_usadas"></activity>
        <activity android:name=".VistaErrores"
            android:label="@string/errores"></activity>
        <activity android:name=".VistaOcurrencias"
            android:label="@string/report_ocurrencia_short"></activity>
        <activity android:name=".VistaAnimaciones"
            android:label="@string/report_animacion"></activity>
        <activity
            android:name=".DisplayMessageActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.Practica1.NoActionBar"></activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.Practica1.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>